<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network Access for YouTube Music and LRCLIB APIs -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Background Audio Playback & Foreground Services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Tactile Micro-Feedback for Scrolling and Snapping -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- In-App Over-The-Air (OTA) Software Updates Installer Permission -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".HunterApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.HUNTERxMUSIC.Launch">

        <!-- Background Playback MediaSession Service -->
        <service
            android:name=".service.MusicPlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
            <meta-data
                android:name="androidx.media3.session.media_notification_small_icon"
                android:resource="@drawable/ic_notification" />
        </service>

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Universal Deep Linking for cyrosonic.com Tracks & Playlists -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="cyrosonic.com"
                    android:pathPrefix="/track" />
                <data
                    android:scheme="https"
                    android:host="www.cyrosonic.com"
                    android:pathPrefix="/track" />
                <data
                    android:scheme="https"
                    android:host="cyrosonic.com"
                    android:pathPrefix="/song" />
                <data
                    android:scheme="https"
                    android:host="www.cyrosonic.com"
                    android:pathPrefix="/song" />
                <data
                    android:scheme="https"
                    android:host="cyrosonic.com"
                    android:pathPrefix="/party" />
                <data
                    android:scheme="https"
                    android:host="www.cyrosonic.com"
                    android:pathPrefix="/party" />
                <data
                    android:scheme="cyrosonic"
                    android:host="track" />
                <data
                    android:scheme="cyrosonic"
                    android:host="party" />
            </intent-filter>
        </activity>

        <!-- Secure FileProvider for In-App OTA APK Package Installations -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".widget.HuntrWidget"
            android:exported="false"
            android:label="CyroSonic — Lyrics / Quotes / Clock">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_huntr_info" />
        </receiver>
    </application>

    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
</manifest>
